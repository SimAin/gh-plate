"""The ``issues`` domain: status tables for open GitHub issues — the ``plate
issues`` subcommand. Builds on :mod:`plate.core`; never imports a sibling
domain package (see ``tests/test_boundaries.py``).
"""
