"""The retro domain: a retrospective panel of the viewer's own GitHub
activity — reviews, commits, PRs opened and closed — day-bucketed over a
recent window.

Viewer-scoped and repo-free: it answers "what have I been doing?", not
"what's on my plate?", so it needs no checkout and never resolves a repo.
"""
